<%@ page contentType="text/html; charset=UTF-8" pageEncoding="UTF-8" %>
<%@ taglib prefix="c" uri="jakarta.tags.core" %>
<!doctype html><html lang="vi"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Danh sách danh mục | KhangGear</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"></head><body class="bg-light">
<nav class="navbar navbar-dark bg-primary"><div class="container"><a class="navbar-brand" href="${pageContext.request.contextPath}/admin">KhangGear Admin</a><form method="post" action="${pageContext.request.contextPath}/logout"><input type="hidden" name="${_csrf.parameterName}" value="${_csrf.token}"><button class="btn btn-outline-light" type="submit">Đăng xuất</button></form></div></nav>
<main class="container py-4"><div class="d-flex justify-content-between align-items-center mb-3"><div><a href="${pageContext.request.contextPath}/admin">Quản trị</a><h1 class="h3 mb-0">Danh mục sản phẩm</h1></div><a class="btn btn-primary" href="${pageContext.request.contextPath}/admin/category/add">+ Thêm danh mục</a></div>
<c:if test="${not empty message}"><div class="alert alert-success">${message}</div></c:if><c:if test="${not empty error}"><div class="alert alert-danger">${error}</div></c:if>
<form class="row g-2 mb-3" method="get" action="${pageContext.request.contextPath}/admin/category/list"><div class="col-md-6"><input class="form-control" name="keyword" value="${keyword}" placeholder="Tìm danh mục..."></div><div class="col-auto"><button class="btn btn-secondary" type="submit">Tìm</button></div></form>
<div class="card shadow-sm"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th>#</th><th>Icon</th><th>Tên danh mục</th><th>Mô tả</th><th>Trạng thái</th><th class="text-end">Thao tác</th></tr></thead><tbody><c:choose><c:when test="${empty categories.content}"><tr><td colspan="6" class="text-center py-4 text-muted">Chưa có danh mục.</td></tr></c:when><c:otherwise><c:forEach var="category" items="${categories.content}" varStatus="s"><tr><td>${s.count}</td><td><c:if test="${not empty category.icon}"><img src="${pageContext.request.contextPath}/media/categories/${category.icon}" alt="${category.name}" width="48" height="48" class="rounded object-fit-cover"></c:if></td><td>${category.name}</td><td>${category.description}</td><td>${category.active ? 'Đang hoạt động' : 'Đã ẩn'}</td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="${pageContext.request.contextPath}/admin/category/edit?id=${category.id}">Sửa</a> <form class="d-inline" method="post" action="${pageContext.request.contextPath}/admin/category/delete" onsubmit="return confirm('Bạn có chắc muốn xóa danh mục này?');"><input type="hidden" name="${_csrf.parameterName}" value="${_csrf.token}"><input type="hidden" name="id" value="${category.id}"><button class="btn btn-sm btn-outline-danger" type="submit">Xóa</button></form></td></tr></c:forEach></c:otherwise></c:choose></tbody></table></div></div></main></body></html>
