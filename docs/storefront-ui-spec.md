# Storefront UI

Storefront React dung namespace CSS `kg-`, layout responsive cho desktop/tablet/mobile, gom header tim kiem, hero, trust strip, danh muc va catalog card. Cac route da co: `/`, `/products`, `/account`, `/orders`, `/cart`; account/cart/order se duoc ket noi nghiep vu o giai doan Commerce.
